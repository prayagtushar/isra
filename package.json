{
  "name": "isra",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "ingest": "uv run --directory apps/ingest python -m src",
    "eval": "uv run --directory apps/evals python -m src",
    "gen:contracts": "bun run --cwd packages/contracts gen",
    "dev:api": "uv run --directory apps/api uvicorn src.main:app --reload --port ${API_PORT:-8000}",
    "dev:web": "bun run --cwd apps/web dev"
  },
  "devDependencies": {
    "turbo": "^2.9.18"
  },
  "packageManager": "bun@1.3.14"
}
