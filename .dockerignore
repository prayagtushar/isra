# The build context is the repo root, so keep it to the uv workspace packages.

# VCS / tooling
.git
.gitignore
.github
.claude
.remember
.turbo

# Python virtualenvs / caches (rebuilt inside the image)
.venv
**/.venv
**/__pycache__
**/*.pyc
.pytest_cache
.ruff_cache

# JS — not needed for the Python API image
node_modules
**/node_modules
apps/web
bun.lock

# Data, build outputs, notebooks, docs
data
dist
**/dist
**/.next
notebooks
docs

# Local env / secrets — never bake these into the image
.env
**/.env
.env.*
**/.env.*

# Misc
*.log
.DS_Store
